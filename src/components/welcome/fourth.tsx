import { defineComponent } from "vue";

export const Fourth = defineComponent({
    setup: (props, context)=> {
        return () => (
            <div>
                Fourth
            </div>
        )
    }
})